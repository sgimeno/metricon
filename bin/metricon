#!/usr/bin/env node

require('commander')
  .version(require('../package').version)
  .usage('<command> [options]')
  .command('report', 'generate a milestone report')
  .command('retro', 'add retrospective feedback')
  .parse(process.argv)
