{
  "name": "metricon",
  "version": "1.0.0",
  "description": "",
  "main": "metricon.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/sgimeno/metricon.git"
  },
  "scripts": {
    "build:web": "browserify index.js --standalone metricon -t envify > bin/metricon.js",
    "dev": "tape-watch ./test/unit/**/*.js",
    "precommit": "standard --fix",
    "prepush": "npm test",
    "postinstall": "npm run build:web",
    "semantic-release": "semantic-release pre && npm publish && semantic-release post",
    "test:cli": "./bin/metricon report -O sgimeno -R metricon -M \"Test milestone\" -B sgimeno/metricon",
    "test:unit": "tape ./test/unit/**/*.js",
    "test": "npm run test:unit && npm run test:cli"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "commander": "^2.9.0",
    "github-api": "^3.0.0",
    "lodash.flatten": "^4.4.0",
    "lodash.groupby": "^4.6.0",
    "request": "^2.81.0",
    "request-promise-native": "^1.0.3"
  },
  "devDependencies": {
    "browserify": "^14.3.0",
    "envify": "^4.0.0",
    "husky": "^0.13.3",
    "semantic-release": "^6.3.2",
    "standard": "^9.0.2",
    "tape": "^4.6.3",
    "tape-watch": "^2.3.0"
  }
}
